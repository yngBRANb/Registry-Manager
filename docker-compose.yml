version: '3'
services:
    web:
        image: register-manager
        restart: always
        ports:
            - 8553:8080
        volumes:
            - /home/registry/media:./media
            - /home/registry/db/:./db.sqlite3
